import { meta } from './meta'

// Placeholder template. Replace with real sections once the design lands.
export function Vector() {
  return (
    <section className="min-h-screen bg-surface p-8 text-on-surface">
      <h1 className="text-3xl font-semibold">{meta.name}</h1>
    </section>
  )
}
